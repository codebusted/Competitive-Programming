#include <iostream>
#include <vector>

int main(int argc, char const *argv[])
{
	int t, n;
	std::vector<int> v;
	std::cin >> t;
	while(t--) {
		std::cin >> n;
		v.clear();
		for(auto i = v.begin(); i != v.end(); i++)
			std::cin >> *i;
		
	}
	return 0;
}