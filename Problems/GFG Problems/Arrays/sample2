#include <iostream>

using namespace std;

int sample() {
	cin.clear();
    cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');

	int n, k;
	cin >> n;

	int arr[n + 100];
	for (int i = 0; i < n; ++i) {
	    cin >> arr[i];
	}


	return 0;
}

int main(int argc, char const *argv[])
{
	int T;
	cin >> T;

	int ite = 0;
	while(ite < T) {
		
		sample();

		ite ++;
	}

	return 0;
}