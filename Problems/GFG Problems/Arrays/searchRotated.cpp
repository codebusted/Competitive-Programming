#include <iostream>

using namespace std;

int searchRotate() {
	cin.clear();
    cin.ignore(std::numeric_limits<std::streamsize>::max(), '\n');

	int n, k;
	cin >> n;

	int arr[n];
	for (int i = 0; i < n; ++i) {
	    cin >> arr[i];
	}
	cin >> k;




	return -1;
}

int main(int argc, char const *argv[])
{
	int T;
	cin >> T;

	int ite = 0;
	while(ite < T) {
		
		searchRotate();

		ite ++;
	}

	return 0;
}